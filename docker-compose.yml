version: '3.8'
services:
  database:
    container_name: database
    image: postgres:14
    ports:
      - '5432'
    environment:
      POSTGRES_PASSWORD: postgres
  app:
    container_name: app
    build: .
    image: app
    depends_on:
      - database
    ports:
      - '8080:8080'